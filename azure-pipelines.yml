pool:
  name: TEST

steps:
- task: MSBuild@1
  inputs:
    solution: 'AnyStore.sln'
    platform: 'Any CPU'
    configuration: 'Release'
    msbuildArguments: '/t:Rebuild /p:VisualStudioVersion=latest'

